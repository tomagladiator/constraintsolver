'use strict';

Object.defineProperty(exports, '__esModule', {
    value: true
});

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) arr2[i] = arr[i]; return arr2; } else { return Array.from(arr); } }

var _objectAssign = require('object-assign');

var _objectAssign2 = _interopRequireDefault(_objectAssign);

var addAssertions = function addAssertions(tape, assertions) {
    var myTape = function myTape() {
        for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
            args[_key] = arguments[_key];
        }

        args = args.map(function (arg) {
            if (typeof arg === 'function') {
                return function (t) {
                    Object.keys(assertions).forEach(function (assertName) {
                        return t[assertName] = assertions[assertName].bind(t);
                    });

                    t.test = addAssertions(t.test, assertions);

                    arg.call(t, t);
                };
            } else {
                return arg;
            }
        });
        tape.apply(undefined, _toConsumableArray(args));
    };

    return (0, _objectAssign2['default'])(myTape, tape);
};

exports['default'] = addAssertions;
module.exports = exports['default'];
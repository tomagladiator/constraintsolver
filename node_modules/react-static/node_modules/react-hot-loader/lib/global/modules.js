"use strict";

exports.__esModule = true;
const openedModules = {};

const isOpened = exports.isOpened = sourceModule => !!openedModules[sourceModule.id];

const enter = exports.enter = sourceModule => {
  const moduleId = sourceModule.id;
  openedModules[moduleId] = true;
};

const leave = exports.leave = sourceModule => {
  delete openedModules[sourceModule.id];
};